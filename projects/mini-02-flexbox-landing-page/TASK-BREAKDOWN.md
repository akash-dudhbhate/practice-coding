# Landing Page with Flexbox — Task Breakdown

> **Step-by-step implementation guide.** Follow each step in order.

---

## File Structure

```
mini-02-flexbox-landing-page/
├── index.html, styles.css, script.js (smooth scroll)
└── README.md
```

---

## Implementation Steps

### Step 1: HTML Skeleton

Create index.html with header, hero, features, testimonials, CTA, footer. Use semantic tags.

**Checkpoint:** Step 1 is complete when the described functionality works.

### Step 2: Header + Nav

Sticky header with logo and navigation links. Use flexbox for layout.

**Checkpoint:** Step 2 is complete when the described functionality works.

### Step 3: Hero Section

Full-width hero with headline, subtext, and CTA button. Flexbox centering.

**Checkpoint:** Step 3 is complete when the described functionality works.

### Step 4: Features Grid

3-column flexbox grid with icons, titles, descriptions. Use flex-wrap for responsive.

**Checkpoint:** Step 4 is complete when the described functionality works.

### Step 5: Testimonials

Row of 3 testimonial cards with quotes, names, avatars. Flexbox with gap.

**Checkpoint:** Step 5 is complete when the described functionality works.

### Step 6: CTA Section

Centered call-to-action with button. Background color contrast.

**Checkpoint:** Step 6 is complete when the described functionality works.

### Step 7: Footer

Multi-column footer with links, social icons, copyright.

**Checkpoint:** Step 7 is complete when the described functionality works.

### Step 8: CSS Styling

Add colors, typography, spacing, hover effects. Use CSS variables for brand colors.

**Checkpoint:** Step 8 is complete when the described functionality works.

### Step 9: Responsive

Media queries: mobile (1 column), tablet (2 columns), desktop (3 columns).

**Checkpoint:** Step 9 is complete when the described functionality works.

### Step 10: Smooth Scroll

JS for smooth scroll on nav link clicks. Active link highlighting.

**Checkpoint:** Step 10 is complete when the described functionality works.

---

## Final Checklist

- [ ] Hero section with headline + CTA
- [ ] Features section (3+ items) with flexbox
- [ ] Testimonials section
- [ ] CTA section
- [ ] Responsive (mobile, tablet, desktop)
- [ ] Smooth scroll navigation
- [ ] Hover effects on buttons and cards
- [ ] Consistent spacing and typography

---

## Common Pitfalls

1. **Skipping steps** — each step builds on the previous. Don't jump ahead.
2. **Not testing incrementally** — test after each step, not just at the end.
3. **Ignoring error states** — handle empty states, loading, and errors from the start.
4. **Not making it responsive** — test on mobile from the beginning, not as an afterthought.
