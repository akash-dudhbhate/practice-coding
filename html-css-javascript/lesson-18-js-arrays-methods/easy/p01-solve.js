/**
LESSON 18 — JavaScript Arrays & Methods
========================================

PROBLEM: map / filter / reduce (Easy)
The big three array methods on one array of numbers.

TRY THIS:
  - const nums = [1, 2, 3, 4, 5].
  - nums.map(n => n * 2) to double each.
  - .filter(n => n % 2 === 0) to keep evens.
  - .reduce((acc, n) => acc + n, 0) to sum.
  - console.log all three results.

EXPECTED OUTPUT:
  doubled [2,4,6,8,10], evens [2,4,6,8,10], sum 30

TEST: node easy/p01-solve.js

CHECK: python3 check.py easy/p01
*/

// TODO: Write your complete solution from scratch below.
