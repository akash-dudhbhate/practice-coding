/**
LESSON 18 — JavaScript Arrays & Methods
========================================

PROBLEM: push / pop / shift / unshift (Easy)
Add and remove from both ends of a fruits array.

TRY THIS:
  - let fruits = ["apple", "banana", "cherry"].
  - push("date"), pop(), unshift("avocado"), shift() —
    console.log the array after EACH operation.

EXPECTED OUTPUT: Four logs showing the array changing ends:
  [..., "date"] -> back to 3 -> ["avocado", ...] -> back to original.

TEST: node easy/p02-solve.js

CHECK: python3 check.py easy/p02
*/

// TODO: Write your complete solution from scratch below.
