/**
LESSON 18 — JavaScript Arrays & Methods
========================================

PROBLEM: find() a User (Easy)
Write findUser(users, id) returning the user or "Not found".

TRY THIS:
  - users.find(u => u.id === id).
  - Return the user object, or "Not found" when undefined.
  - Test with a hit (id 2) and a miss (id 99).

EXPECTED OUTPUT:
  findUser(users, 2)  -> { id: 2, name: "Bob" }
  findUser(users, 99) -> "Not found"

TEST: node easy/p03-solve.js

CHECK: python3 check.py easy/p03
*/

// TODO: Write your complete solution from scratch below.
