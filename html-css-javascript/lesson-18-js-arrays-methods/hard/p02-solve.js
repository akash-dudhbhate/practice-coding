/**
LESSON 18 — JavaScript Arrays & Methods
========================================

PROBLEM: Flatten + Unique + Sort (Hard)
Write flattenAndUnique(nestedArr) for arbitrary nesting.

TRY THIS:
  - nestedArr.flat(Infinity) to flatten fully.
  - new Set(...) to dedupe, spread back to an array.
  - .sort((a, b) => a - b) for numeric order.

EXPECTED OUTPUT:
  flattenAndUnique([[1,2],[3,[4,2]],[1,5]]) === [1,2,3,4,5]

TEST: node hard/p02-solve.js

CHECK: python3 check.py hard/p02
*/

// TODO: Write your complete solution from scratch below.
