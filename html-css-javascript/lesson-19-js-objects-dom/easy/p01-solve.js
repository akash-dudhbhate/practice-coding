/**
LESSON 19 — JavaScript Objects & DOM
=====================================

PROBLEM: Object with a Method (Easy)
Create a car object whose method uses this.

TRY THIS:
  - const car = { brand: "Toyota", model: "Camry", year: 2024,
    getInfo() { return `${this.brand} ${this.model} (${this.year})`; } }.
  - console.log(car.getInfo()).

EXPECTED OUTPUT:
  Toyota Camry (2024)

TEST: node easy/p01-solve.js

CHECK: python3 check.py easy/p01
*/

// TODO: Write your complete solution from scratch below.
