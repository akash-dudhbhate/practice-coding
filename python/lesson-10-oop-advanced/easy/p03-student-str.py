"""
LESSON 10 — OOP Advanced
EASY P03 — Student __str__
============================================

CONCEPT:
  `__str__` is the "dunder" (double-underscore) method Python calls
  for str(obj) and print(obj) — define it for readable objects.

PROBLEM:
  Write a `Student` class with `__init__(name, grade)` and a
  `__str__` method returning "Student: Name (Grade)".

TRY THIS INPUT:
  ```python
  s = Student("Akash", "A")
  print(str(s))
  ```

EXPECTED OUTPUT:
  ```
  Student: Akash (A)
  ```

CHECK: python3 check.py easy/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
