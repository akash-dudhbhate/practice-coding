"""
LESSON 10 — OOP Advanced
MEDIUM P01 — Vector Class
============================================

CONCEPT:
  Operator overloading: `__add__` powers `+`, `__sub__` powers `-`,
  `__eq__` powers `==`. Each returns a new object (or a bool).

PROBLEM:
  Write a `Vector` class with `__init__(x, y)` and the dunder
  methods `__add__`, `__sub__`, `__eq__`, and `__str__` returning
  "Vector(x, y)".

TRY THIS INPUT:
  ```python
  v1 = Vector(1, 2)
  v2 = Vector(3, 4)
  print(v1 + v2)
  print(v2 - v1 == Vector(2, 2))
  ```

EXPECTED OUTPUT:
  ```
  Vector(4, 6)
  True
  ```

CHECK: python3 check.py medium/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
