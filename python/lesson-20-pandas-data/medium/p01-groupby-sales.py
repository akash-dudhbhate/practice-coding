"""
LESSON 20 — Pandas & Data
MEDIUM P01 — GroupBy Sales Analysis
============================================

CONCEPT:
  `df.groupby("col")["val"].sum()` buckets rows and aggregates —
  SQL's GROUP BY in one line.

PROBLEM:
  Write a `main()` that builds a sales DataFrame (product, region,
  sales, date), then prints total sales by region and average sales
  by product using groupby.

TRY THIS INPUT:
  ```python
  main()
  ```

EXPECTED OUTPUT:
  ```
  Total sales by region:
  region
  East     640
  North    430
  South    450
  ...
  ```

CHECK: python3 check.py medium/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
