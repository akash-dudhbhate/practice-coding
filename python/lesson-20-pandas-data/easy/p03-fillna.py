"""
LESSON 20 — Pandas & Data
EASY P03 — Fill NaN Values
============================================

CONCEPT:
  Real data has holes (NaN). `fillna()` patches them — numbers often
  get the mean, strings get a placeholder like "Unknown".

PROBLEM:
  Write a `main()` that builds a DataFrame with some NaN values in
  `age` and `city`, prints it before, fills NaN ages with the mean
  age and NaN cities with "Unknown", then prints it after.

TRY THIS INPUT:
  ```python
  main()
  ```

EXPECTED OUTPUT:
  ```
  Before:
       name   age    city
  1     Bob   NaN     NaN
  After:
       name   age      city
  1     Bob  30.0  Unknown
  ```

CHECK: python3 check.py easy/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
