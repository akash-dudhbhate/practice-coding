"""
LESSON 20 — Pandas & Data
EASY P01 — DataFrame Basics
============================================

CONCEPT:
  A DataFrame is a table: dict keys become columns, lists become
  rows. `.shape`, `.columns`, and `.head(n)` inspect it.

PROBLEM:
  Write a `main()` function that builds a DataFrame from a dict of
  5 people (name, age, city) and prints its shape, columns, and
  first 3 rows.

TRY THIS INPUT:
  ```python
  main()
  ```

EXPECTED OUTPUT:
  ```
  Shape: (5, 3)
  Columns: ['name', 'age', 'city']
  First 3 rows:
       name  age      city
  0   Alice   25   Mumbai
  ...
  ```

CHECK: python3 check.py easy/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
