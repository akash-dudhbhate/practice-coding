"""
LESSON 02 — Strings & String Methods
EASY P03 — Palindrome Check
============================================

CONCEPT:
  A palindrome reads the same forwards and backwards. Comparing a string
  to its reverse (s == s[::-1]) is the classic test. Normalizing first —
  lowercasing and removing spaces — makes the check fair for real phrases.

PROBLEM:
  Write a function `is_palindrome(s: str) -> bool` that returns True if
  `s` reads the same forwards and backwards. Ignore case and spaces.
  An empty string counts as a palindrome.

TRY THIS INPUT:
  ```python
  print(is_palindrome("racecar"))
  print(is_palindrome("hello"))
  print(is_palindrome("A Santa at NASA"))
  ```

EXPECTED OUTPUT:
  ```
  True
  False
  True
  ```

CHECK: python3 check.py easy/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
