"""
PROBLEM: Extract Email Domain (Hard)
======================================

CONCEPT: .find(), .split(), string slicing, edge case handling.

# See concepts.md in this lesson folder for detailed explanations (WHY/WHERE/WHAT-GOES-WRONG).

Write a function `extract_domain(email)` that takes an email address
and returns the domain part (everything after the @ symbol).

If there is no @ in the string, return an empty string "".

Examples:
    extract_domain("user@gmail.com")          -> "gmail.com"
    extract_domain("akash.kumar@yahoo.co.in") -> "yahoo.co.in"
    extract_domain("a@b.io")                  -> "b.io"
    extract_domain("no-at-sign")              -> ""

"""

# TODO: Write your complete solution from scratch below (function signature + body).
#       Remove this TODO line when done.
