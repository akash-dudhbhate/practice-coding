"""
LESSON 02 — Strings & String Methods
HARD P01 — Anagram Check
============================================

CONCEPT:
  Two words are anagrams if they contain exactly the same letters in
  possibly different orders. sorted() turns a string into a sorted list
  of characters, so comparing sorted(s1) == sorted(s2) detects anagrams.

PROBLEM:
  Write a function `is_anagram(s1: str, s2: str) -> bool` that returns
  True if the two strings are anagrams. Ignore case and spaces, so
  "Dormitory" and "Dirty room" count as anagrams.

TRY THIS INPUT:
  ```python
  print(is_anagram("listen", "silent"))
  print(is_anagram("hello", "world"))
  print(is_anagram("Dormitory", "Dirty room"))
  ```

EXPECTED OUTPUT:
  ```
  True
  False
  True
  ```

CHECK: python3 check.py hard/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
