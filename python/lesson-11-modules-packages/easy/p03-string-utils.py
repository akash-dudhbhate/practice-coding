"""
LESSON 11 — Modules & Packages
EASY P03 — Reusable String Utilities Module
============================================

CONCEPT:
  Modules let you group related helper functions in one file and reuse
  them everywhere. Keeping functions at the top level of a file makes
  them importable: `from string_utils import reverse_string`.

PROBLEM:
  In this file, define two functions:
    - `reverse_string(s)` — returns the string reversed
    - `count_vowels(s)` — returns the number of vowels (a, e, i, o, u,
      case-insensitive) in the string
  Add an `if __name__ == "__main__":` block that tests both.

TRY THIS INPUT:
  ```python
  print(reverse_string("hello"))
  print(count_vowels("hello"))
  ```

EXPECTED OUTPUT:
  ```
  olleh
  2
  ```

CHECK: python3 check.py easy/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
