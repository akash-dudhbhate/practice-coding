"""
LESSON 03 — Lists & Tuples
EASY P01 — Sum List
============================================

CONCEPT:
  A for loop can walk a list one element at a time and accumulate a
  running total. Writing this by hand teaches you what the built-in
  sum() does under the hood.

PROBLEM:
  Write a function `sum_list(nums: list) -> int` that returns the sum
  of all numbers in `nums` WITHOUT using the built-in sum().
  Empty list -> 0.

TRY THIS INPUT:
  ```python
  print(sum_list([1, 2, 3]))
  print(sum_list([]))
  print(sum_list([-1, 1]))
  ```

EXPECTED OUTPUT:
  ```
  6
  0
  0
  ```

CHECK: python3 check.py easy/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
