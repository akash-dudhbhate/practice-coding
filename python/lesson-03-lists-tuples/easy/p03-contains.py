"""
LESSON 03 — Lists & Tuples
EASY P03 — Contains
============================================

CONCEPT:
  Linear search is the simplest lookup: walk the list, compare each
  element to the target, and return True the moment you match. If the
  loop finishes, the target was never there.

PROBLEM:
  Write a function `contains(items: list, target) -> bool` that
  returns True if `target` is in `items`, False otherwise. Do NOT use
  the `in` operator — loop manually.

TRY THIS INPUT:
  ```python
  print(contains([1, 2, 3], 2))
  print(contains([1, 2, 3], 5))
  print(contains([], 1))
  ```

EXPECTED OUTPUT:
  ```
  True
  False
  False
  ```

CHECK: python3 check.py easy/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
