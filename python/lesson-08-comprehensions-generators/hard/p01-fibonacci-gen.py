"""
LESSON 08 — Comprehensions & Generators
HARD P01 — Fibonacci Generator
============================================

CONCEPT:
  A function containing `yield` becomes a generator — it produces
  one value at a time and pauses between calls, remembering its
  state.

PROBLEM:
  Write a generator function `fibonacci_gen(n)` that yields the
  first n Fibonacci numbers (starting 0, 1, 1, 2, 3, 5...).

TRY THIS INPUT:
  ```python
  print(list(fibonacci_gen(6)))
  print(list(fibonacci_gen(0)))
  ```

EXPECTED OUTPUT:
  ```
  [0, 1, 1, 2, 3, 5]
  []
  ```

CHECK: python3 check.py hard/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
