"""
LESSON 08 — Comprehensions & Generators
EASY P02 — Evens Comprehension
============================================

CONCEPT:
  Add `if condition` at the end of a comprehension to keep only the
  items that pass: `[x for x in items if x % 2 == 0]`.

PROBLEM:
  Write a function `evens(nums)` that returns a list of only the
  even numbers from `nums`, using a list comprehension with `if`.

TRY THIS INPUT:
  ```python
  print(evens([1, 2, 3, 4, 5, 6]))
  print(evens([1, 3, 5]))
  ```

EXPECTED OUTPUT:
  ```
  [2, 4, 6]
  []
  ```

CHECK: python3 check.py easy/p02
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
