"""
LESSON 05 — Control Flow & Loops
EASY P01 — Classify Number
============================================

CONCEPT:
  if / elif / else lets your code pick exactly one branch based on a
  condition. Ordering matters: check the cases one at a time and the
  first matching branch wins.

PROBLEM:
  Write a function `classify_number(n: int) -> str` that returns
  "positive" if n > 0, "negative" if n < 0, and "zero" if n == 0.

TRY THIS INPUT:
  ```python
  print(classify_number(5))
  print(classify_number(-3))
  print(classify_number(0))
  ```

EXPECTED OUTPUT:
  ```
  positive
  negative
  zero
  ```

CHECK: python3 check.py easy/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
