"""
LESSON 05 — Control Flow & Loops
EASY P03 — Count Down
============================================

CONCEPT:
  A while loop repeats as long as its condition stays true. Counting
  down means decrementing the counter inside the loop — forget that
  step and you get an infinite loop.

PROBLEM:
  Write a function `count_down(n: int) -> list` that returns a list
  counting down from n to 1 using a while loop.
  count_down(3) -> [3, 2, 1]. count_down(0) -> [].

TRY THIS INPUT:
  ```python
  print(count_down(3))
  print(count_down(1))
  print(count_down(0))
  ```

EXPECTED OUTPUT:
  ```
  [3, 2, 1]
  [1]
  []
  ```

CHECK: python3 check.py easy/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
