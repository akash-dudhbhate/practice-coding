"""
LESSON 05 — Control Flow & Loops
MEDIUM P02 — Skip Negatives
============================================

CONCEPT:
  `continue` jumps to the next loop iteration, skipping the rest of the
  body. It's the natural way to filter: skip the items you don't want,
  keep the ones you do.

PROBLEM:
  Write a function `skip_negatives(nums: list) -> list` that returns a
  new list of only the non-negative numbers, using `continue` to skip
  negatives. Keep the original order.

TRY THIS INPUT:
  ```python
  print(skip_negatives([1, -2, 3, -4, 5]))
  print(skip_negatives([-1, -2]))
  print(skip_negatives([]))
  ```

EXPECTED OUTPUT:
  ```
  [1, 3, 5]
  []
  []
  ```

CHECK: python3 check.py medium/p02
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
