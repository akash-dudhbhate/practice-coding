"""
LESSON 05 — Control Flow & Loops
MEDIUM P01 — First Even
============================================

CONCEPT:
  Once you find what you're looking for, there's no reason to keep
  looping — an early `return` (or `break`) exits immediately. If the
  loop finishes with no match, fall through to a default like None.

PROBLEM:
  Write a function `first_even(nums: list)` that returns the first
  even number in the list (exit early once found). Return None if the
  list has no even numbers.

TRY THIS INPUT:
  ```python
  print(first_even([1, 3, 4, 5]))
  print(first_even([1, 3, 5]))
  print(first_even([]))
  ```

EXPECTED OUTPUT:
  ```
  4
  None
  None
  ```

CHECK: python3 check.py medium/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
