"""
LESSON 05 — Control Flow & Loops
MEDIUM P03 — Multiplication Table
============================================

CONCEPT:
  Nested loops generate grids: the outer loop picks a row, the inner
  loop fills its cells. A list of lists is the standard Python way to
  represent a 2-D table.

PROBLEM:
  Write a function `multiplication_table(n: int) -> list` that returns
  an n x n multiplication table as a list of lists, where cell [i][j]
  holds (i+1) * (j+1). multiplication_table(3) ->
  [[1,2,3],[2,4,6],[3,6,9]].

TRY THIS INPUT:
  ```python
  print(multiplication_table(3))
  print(multiplication_table(1))
  ```

EXPECTED OUTPUT:
  ```
  [[1, 2, 3], [2, 4, 6], [3, 6, 9]]
  [[1]]
  ```

CHECK: python3 check.py medium/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
