"""
LESSON 05 — Control Flow & Loops
HARD P03 — Print Multiplication Table
============================================

CONCEPT:
  f-string format specifiers like f"{x:3}" right-align a value in a
  fixed width, and print(..., end="") keeps output on one line. Column
  width based on the biggest value (n*n) keeps every row aligned.

PROBLEM:
  Write a function `print_multiplication_table(n: int) -> None` that
  PRINTS a formatted n x n multiplication table (values 1..n on both
  axes). Each cell is right-aligned in a column width of
  len(str(n * n)) + 1 so the table stays aligned.

TRY THIS INPUT:
  ```python
  print_multiplication_table(3)
  ```

EXPECTED OUTPUT:
  ```
   1 2 3
   2 4 6
   3 6 9
  ```

CHECK: python3 check.py hard/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
