"""
LESSON 06 — Functions Deep Dive
MEDIUM P03 — Apply Func
============================================

CONCEPT:
  Functions are values — you can pass them as arguments. A function
  that takes another function is called a higher-order function.

PROBLEM:
  Write a function `apply_func(func, items)` that returns a new list
  with `func` applied to each item (like `map`, but build the list
  yourself).

TRY THIS INPUT:
  ```python
  print(apply_func(lambda x: x * 2, [1, 2, 3]))
  print(apply_func(str, [1, 2]))
  ```

EXPECTED OUTPUT:
  ```
  [2, 4, 6]
  ['1', '2']
  ```

CHECK: python3 check.py medium/p03
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
