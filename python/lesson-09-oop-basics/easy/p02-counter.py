"""
LESSON 09 — OOP Basics
EASY P02 — Counter Class
============================================

CONCEPT:
  Instance attributes live on `self` and persist between method
  calls — each object keeps its own state.

PROBLEM:
  Write a class `Counter` whose `__init__` sets count to 0, with
  `increment()` adding 1 and `get()` returning the current count.

TRY THIS INPUT:
  ```python
  c = Counter()
  c.increment()
  c.increment()
  print(c.get())
  ```

EXPECTED OUTPUT:
  ```
  2
  ```

CHECK: python3 check.py easy/p02
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
