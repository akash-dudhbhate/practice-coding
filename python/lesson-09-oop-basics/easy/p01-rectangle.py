"""
LESSON 09 — OOP Basics
EASY P01 — Rectangle Class
============================================

CONCEPT:
  A class bundles data (attributes) and behavior (methods).
  `__init__` runs when you create an instance and stores values on
  `self`.

PROBLEM:
  Write a class `Rectangle` with `__init__(self, width, height)`,
  a method `area()` returning width*height, and a method
  `perimeter()` returning 2*(width+height).

TRY THIS INPUT:
  ```python
  r = Rectangle(4, 5)
  print(r.area())
  print(r.perimeter())
  ```

EXPECTED OUTPUT:
  ```
  20
  18
  ```

CHECK: python3 check.py easy/p01
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
