"""
LESSON — Cross Validation Tuning
HARD P03 — Build a model selection script: compare 3 models (logistic regression, random forest, gradient boosting) using 5-fold CV with 3 metrics (accuracy, F1, AUC). For each model, also tune one key hyperparameter. Print a final comparison table with best model per metric.
==================================================

CONCEPT:
  See concepts.md in this lesson folder for detailed explanations.

PROBLEM:
  Build a model selection script: compare 3 models (logistic regression, random forest, gradient boosting) using 5-fold CV with 3 metrics (accuracy, F1, AUC). For each model, also tune one key hyperparameter. Print a final comparison table with best model per metric


Write a function `solve()` that implements the solution.

# Check your answer: compare with solutions/p03-solution.py
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
