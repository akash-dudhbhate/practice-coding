"""
LESSON — Cross Validation Tuning
HARD P01 — Build a complete tuning pipeline: load a real dataset, create a pipeline (scaler + model), use `GridSearchCV` with 3+ hyperparameters, print best params, evaluate on held-out test set, and compare with default hyperparameters.
==================================================

CONCEPT:
  See concepts.md in this lesson folder for detailed explanations.

PROBLEM:
  Build a complete tuning pipeline: load a real dataset, create a pipeline (scaler + model), use `GridSearchCV` with 3+ hyperparameters, print best params, evaluate on held-out test set, and compare with default hyperparameters


Write a function `solve()` that implements the solution.

# Check your answer: compare with solutions/p01-solution.py
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
