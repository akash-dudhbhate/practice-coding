"""
LESSON — Cross Validation Tuning
MEDIUM P03 — Build a pipeline with `StandardScaler` + `LogisticRegression`. Tune `C` [0.01, 0.1, 1, 10, 100] using `GridSearchCV`. Verify no data leakage (scaling is inside CV).
==================================================

CONCEPT:
  See concepts.md in this lesson folder for detailed explanations.

PROBLEM:
  Build a pipeline with `StandardScaler` + `LogisticRegression`. Tune `C` [0.01, 0.1, 1, 10, 100] using `GridSearchCV`. Verify no data leakage (scaling is inside CV)


Write a function `solve()` that implements the solution.

# Check your answer: compare with solutions/p03-solution.py
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
