# AI/ML Track Dependencies
# Install: pip install -r requirements.txt
# Levels 00-07 need only the core stack; 08+ need torch/flask/fastapi.

# --- Core (levels 00-07) ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
scipy>=1.10
joblib>=1.3

# --- Deep learning (levels 08-09) ---
# CPU build is enough for all exercises:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision>=0.15

# --- Deployment (level 10) ---
flask>=3.0
fastapi>=0.110
uvicorn>=0.29
httpx2>=0.1        # required by fastapi.testclient

# --- Optional: real LLM APIs (levels 11-13 work without these) ---
# openai>=1.0
# anthropic>=0.40
