"""
LESSON — Model Deployment
MEDIUM P03 — Write tests for the ML API: unit test (model produces valid output), integration test (API responds correctly), edge case test (invalid input returns 422, missing field returns error). Use `pytest` and `TestClient` from FastAPI.
==================================================

CONCEPT:
  See concepts.md in this lesson folder for detailed explanations.

PROBLEM:
  Write tests for the ML API: unit test (model produces valid output), integration test (API responds correctly), edge case test (invalid input returns 422, missing field returns error). Use `pytest` and `TestClient` from FastAPI


Write a function `solve()` that implements the solution.

# Check your answer: compare with solutions/p03-solution.py
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
