"""
MINI PROJECT — After Lesson 19: Model Deployment
==================================================
ML Model API
-------------

Build a complete ML API with deployment:
  1. Train a simple model (iris classifier or similar)
  2. Save the model (pickle or joblib)
  3. Create a FastAPI/Flask API:
     - POST /predict → takes features, returns prediction
     - GET /health → returns model status
     - GET /info → returns model info (version, accuracy, features)
  4. Add input validation
  5. Write a test client that calls the API
  6. Bonus: Dockerize the API
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Build the ML model API.
