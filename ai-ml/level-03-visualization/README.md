# Level 03 Visualization — Data Visualization

## What You'll Learn
- Matplotlib basics
- Seaborn for statistical plots
- Line, bar, scatter, histogram
- Heatmaps and correlation
- Subplots and figure sizing
- Saving figures

## Prerequisites
- Previous levels completed
- Python basics

## How This Level Works

Each problem has:
1. **CONCEPT** — the idea you need to understand
2. **PROBLEM** — what to build
3. **TRY THIS INPUT** — test code to verify your solution
4. **EXPECTED OUTPUT** — what it should print
5. **AUTO-CHECK** — run `check.py` to verify automatically

Start with `easy/` problems, then `medium/`, then `hard/`, then the project.

---

## Problems

### Easy
1. `easy/p01-line-plot.py` — Plot y = sin(x) and y = cos(x) on the same figure with label...
2. `easy/p02-scatter-plot.py` — Create a scatter plot of height vs weight. Color by gender. ...
3. `easy/p03-histogram.py` — Plot a histogram of exam scores. Add mean and median lines. ...

### Medium
4. `medium/p01-correlation-heatmap.py` — Create a correlation heatmap of a dataset. Use seaborn. Anno...
5. `medium/p02-subplots.py` — Create a 2x2 subplot figure with: line plot, scatter, histog...
6. `medium/p03-box-plot.py` — Create a box plot comparing distributions across categories....

### Hard
7. `hard/p01-custom-dashboard.py` — Build a 4-panel dashboard: revenue over time, by product, by...
8. `hard/p02-animated-plot.py` — Create an animated plot showing data changing over time (use...
9. `hard/p03-publication-figure.py` — Create a publication-quality figure with custom fonts, color...

### Project
`project/` — Build a complete data dashboard for a sales dataset with 6 different visualizations.

---

## Verify Your Work

```bash
# Run a problem
python3 easy/p01-line-plot.py

# Check your answer
python3 check.py easy/p01
```

When `check.py` says "PASS", add `# DONE` to the first line and move on.
