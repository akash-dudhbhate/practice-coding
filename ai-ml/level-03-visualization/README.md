# Level 03 — Data Visualization

## What You'll Learn
- matplotlib: lines, scatter, histograms, subplots
- seaborn: heatmaps, box plots, swarm plots
- Multi-panel dashboards
- Animations and publication-quality exports

## Prerequisites
- Level 02 (NumPy, pandas)

## Problems

### Easy
1. `easy/p01-line-plot.py` — `plot_trig()` → sin/cos line plot
2. `easy/p02-scatter-plot.py` — `plot_scatter()` → grouped scatter
3. `easy/p03-histogram.py` — `plot_histogram()` → histogram + stat lines

### Medium
4. `medium/p01-correlation-heatmap.py` — `plot_corr()` → seaborn heatmap
5. `medium/p02-subplots.py` — `plot_dashboard()` → 2×2 subplot grid
6. `medium/p03-box-plot.py` — `plot_box()` → box + swarm overlay

### Hard
7. `hard/p01-custom-dashboard.py` — `plot_sales()` → 4-panel dashboard
8. `hard/p02-animated-plot.py` — `animate()` → FuncAnimation
9. `hard/p03-publication-figure.py` — `plot_publication()` → 300 DPI figure

### Project
`project/` — Build a full sales dashboard with 6+ visualizations.

## Verify

```bash
python3 check.py easy/p01
python3 check.py all
```
