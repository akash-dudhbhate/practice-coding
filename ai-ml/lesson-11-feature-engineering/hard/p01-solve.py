"""
LESSON — Feature Engineering
HARD P01 — Build a complete feature engineering pipeline: load a real dataset (e.g., Titanic from seaborn), handle missing data, encode categoricals, scale numericals, create interaction features, and select top 10 features. Train a model and compare with raw data.
==================================================

CONCEPT:
  See concepts.md in this lesson folder for detailed explanations.

PROBLEM:
  Build a complete feature engineering pipeline: load a real dataset (e.g., Titanic from seaborn), handle missing data, encode categoricals, scale numericals, create interaction features, and select top 10 features. Train a model and compare with raw data


Write a function `solve()` that implements the solution.

# Check your answer: compare with solutions/p01-solution.py
"""

TRY THIS INPUT:
  Add this test code at the bottom of your file:

  ```python
  titanic = sns.load_dataset("titanic")

  # Call your function
  result = solve(...)
  print(result)
  ```

EXPECTED OUTPUT:
  Run the solution file to see expected output:
    python3 hard/solutions/p01-solution.py

  Then compare your output format with theirs.

# === WRITE YOUR CODE BELOW ===
# TODO: Write your solution from scratch.
