"""
MINI PROJECT — After Lesson 17: PyTorch Fundamentals
======================================================
PyTorch Playground
-------------------

Build a simple neural network in PyTorch:
  1. Create a synthetic dataset (X = random features, y = binary classification)
  2. Build a model with nn.Module:
     - Linear layers with ReLU activation
     - Sigmoid output for binary classification
  3. Write a training loop with:
     - Loss function (BCELoss)
     - Optimizer (SGD or Adam)
     - Forward pass, backward pass, weight update
  4. Track loss over epochs
  5. Save and load the model
  6. Compare with sklearn's MLPClassifier
"""

# === WRITE YOUR CODE BELOW ===
# TODO: Build the PyTorch neural network.
