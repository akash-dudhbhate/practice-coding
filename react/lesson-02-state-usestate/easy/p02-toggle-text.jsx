/*
LESSON 02 — State & useState
EASY P02 — Toggle Text
============================================
CONCEPT: Boolean state is the simplest state shape. Toggling it with `!value` flips between two UI states.
PROBLEM: Build a `Toggle` component. It keeps an `isOn` boolean state (start false) and renders a button that shows "ON" or "OFF" and flips the boolean on click.
TRY THIS: Render `<Toggle />` and click the button.
EXPECTED OUTPUT: A button reading "OFF" that switches to "ON" on each click.
CHECK: python3 check.py easy/p02
*/
// TODO: write your component from scratch
