/*
LESSON 02 — State & useState
EASY P01 — Counter
============================================
CONCEPT: useState gives a component memory: a value that survives re-renders. Calling the setter updates the value and tells React to render again.
PROBLEM: Build a `Counter` component (no props). It keeps a `count` state starting at 0 and renders a single button showing the count. Clicking the button calls `setCount(count + 1)`.
TRY THIS: Render `<Counter />` and click the button three times.
EXPECTED OUTPUT: A button reading "Count: 0" that becomes "Count: 3" after three clicks.
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
