/*
LESSON 02 — State & useState
EASY P03 — Text Input Display
============================================
CONCEPT: String state updated in `onChange` makes an input 'controlled': React owns the value, so whatever you type can be echoed anywhere.
PROBLEM: Build a `TextInput` component. Keep a `text` string state. Render an `<input>` whose `value` is the state and whose `onChange` does `setText(e.target.value)`, plus a `<p>` below echoing the text.
TRY THIS: Render `<TextInput />` and type "hello".
EXPECTED OUTPUT: The paragraph under the input shows "hello" as you type.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
