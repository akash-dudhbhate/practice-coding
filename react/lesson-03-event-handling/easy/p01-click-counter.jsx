/*
LESSON 03 — Event Handling
EASY P01 — Click Counter
============================================
CONCEPT: React events are camelCase props (`onClick`) that take a FUNCTION — you pass the handler, React calls it with the event.
PROBLEM: Build a `ClickCounter` component. Keep `count` state and render a button whose `onClick` increments it, displaying the count inside the button.
TRY THIS: Render `<ClickCounter />` and click twice.
EXPECTED OUTPUT: Button text "Clicked: 0" becomes "Clicked: 2".
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
