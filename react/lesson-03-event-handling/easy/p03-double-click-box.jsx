/*
LESSON 03 — Event Handling
EASY P03 — Double-Click Alert
============================================
CONCEPT: One element can carry several handlers at once — `onClick` and `onDoubleClick` fire independently on the same node.
PROBLEM: Build a `DoubleClickBox` component. Keep a `color` state. Render a styled `<div>` (100px box) whose `onClick` toggles the color between two values and whose `onDoubleClick` calls `alert("Double clicked!")`.
TRY THIS: Render `<DoubleClickBox />`, single-click then double-click the box.
EXPECTED OUTPUT: Single click flips the color; double click pops an alert.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
