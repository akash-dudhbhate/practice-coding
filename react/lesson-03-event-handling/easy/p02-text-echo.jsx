/*
LESSON 03 — Event Handling
EASY P02 — Text Echo
============================================
CONCEPT: `onChange` fires on every keystroke and `e.target.value` is the input's current text — the core of every controlled input.
PROBLEM: Build a `TextEcho` component. Keep `text` state; render an input with `onChange={(e) => setText(e.target.value)}` and a `<p>` echoing it.
TRY THIS: Render `<TextEcho />` and type "echo".
EXPECTED OUTPUT: The paragraph mirrors each keystroke instantly.
CHECK: python3 check.py easy/p02
*/
// TODO: write your component from scratch
