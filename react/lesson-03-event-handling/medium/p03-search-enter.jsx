/*
LESSON 03 — Event Handling
MEDIUM P03 — Search with Enter Key
============================================
CONCEPT: `onKeyDown` receives keyboard events; `e.key` tells you which key — "Enter" to submit, "Escape" to clear.
PROBLEM: Build a `SearchEnter` component with `query` and `searched` state. The input is controlled (`value`/`onChange`) and has `onKeyDown`: Enter copies query into `searched` and clears the input; Escape clears both. Show `searched` in a `<p>`.
TRY THIS: Render `<SearchEnter />`, type "react", press Enter, then Escape.
EXPECTED OUTPUT: Enter moves the text to "Search result: react"; Escape clears everything.
CHECK: python3 check.py medium/p03
*/
// TODO: write your component from scratch
