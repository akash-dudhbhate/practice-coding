/*
LESSON 01 — Components, JSX & Props
MEDIUM P01 — User Card
============================================
CONCEPT: Composition means building bigger UI by rendering your own components inside other components, each with different props.
PROBLEM: Build a `UserCard` component taking `name`, `email`, and `role` props. Render a bordered `<div>` containing the name in an `<h3>`, the email in a `<p>`, and the role in a styled `<span>` tag. Then build an `App` component that renders at least 3 `<UserCard>`s with different data — `App` is the component this file provides by default.
TRY THIS: Render `<App />` with cards like `<UserCard name="Alice" email="alice@example.com" role="Admin" />`.
EXPECTED OUTPUT: Three user cards stacked on the page, each showing a name, an email, and a colored role tag.
CHECK: python3 check.py medium/p01
*/
// TODO: write your component from scratch
