/*
LESSON 01 — Components, JSX & Props
EASY P01 — Greeting Component
============================================
CONCEPT: A component is a function that returns JSX. Props arrive as one object argument — destructure it in the signature to read each prop by name.
PROBLEM: Build a `Greeting` component that takes a `name` prop and renders a single `<h1>` reading "Hello, {name}!" — `Greeting` is the component this file provides by default.
TRY THIS: Render `<Greeting name="Dev" />`.
EXPECTED OUTPUT: An h1 in the DOM reading "Hello, Dev!".
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
