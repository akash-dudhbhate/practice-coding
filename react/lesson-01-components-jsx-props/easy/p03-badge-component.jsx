/*
LESSON 01 — Components, JSX & Props
EASY P03 — Badge Component
============================================
CONCEPT: Props can drive logic, not just text. Look a prop up in an object map inside the function body to pick a style before returning JSX.
PROBLEM: Build a `Badge` component taking `text` and `variant` props. Map `variant` ("success", "danger", "warning", "info") to a background color — fall back to "#999" for anything else — and render a pill-shaped `<span>` using `style` that displays `text`.
TRY THIS: Render `<Badge text="New" variant="success" />`.
EXPECTED OUTPUT: A small rounded pill reading "New" on a green background.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
