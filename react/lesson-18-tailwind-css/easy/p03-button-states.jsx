/*
LESSON 18 — Tailwind CSS
EASY P03 — Button State Variants (hover/focus/active)
============================================
CONCEPT: State variants prefix utilities for pseudo-classes: `hover:` on mouse-over, `focus:` on keyboard/click focus, `active:` while pressed. `transition-colors` animates the color change.
PROBLEM: Build `StyledButton` — a `<button>` with base `bg-blue-500 text-white px-6 py-2 rounded transition-colors` plus `hover:bg-blue-600`, `focus:outline-none focus:ring-2 focus:ring-blue-400`, and `active:scale-95`.
TRY THIS: Render `<StyledButton />` — hover it, Tab-focus it (ring appears), hold a click (it shrinks).
EXPECTED OUTPUT: Darker on hover, blue ring on focus, scale-down press effect.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
