/*
LESSON 18 — Tailwind CSS
EASY P01 — Card with Tailwind
============================================
CONCEPT: Tailwind styles via small utility classes directly in `className` — `bg-white rounded-lg shadow-md p-6` replaces a whole CSS file. No class names to invent, no stylesheet to context-switch to.
PROBLEM: Build a `Card` component: a `div` with `bg-white rounded-lg shadow-md p-6 max-w-sm` containing an `<h3>` title (`text-lg font-bold mb-2`), a `<p>` description (`text-gray-600 mb-4`), and a `<button>` styled `bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600`.
TRY THIS: Render `<Card />` — a white card with soft shadow, then hover the button.
EXPECTED OUTPUT: Styled card; button darkens on hover (hover: variant).
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
