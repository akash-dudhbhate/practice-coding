/*
LESSON 06 — useEffect & Side Effects
EASY P02 — Mount Message
============================================
CONCEPT: An empty dependency array `[]` means the effect runs exactly once — after the component's first mount.
PROBLEM: Build a `MountMessage` component. `useEffect(() => { console.log("Component mounted"); }, [])` logs once; render a `<p>` telling the user to check the console.
TRY THIS: Render `<MountMessage />` and open devtools.
EXPECTED OUTPUT: Console shows "Component mounted" exactly once.
CHECK: python3 check.py easy/p02
*/
// TODO: write your component from scratch
