/*
LESSON 05 — Lists & Keys
MEDIUM P03 — Sortable List
============================================
CONCEPT: .sort() MUTATES the array — always sort a copy (`[...names].sort(...)`) so state/props stay untouched.
PROBLEM: Build a `SortableList` component with a `names` constant and `ascending` boolean state. Compute `sorted` by copying then sorting with `localeCompare` in either direction. A button flips `ascending`; render the sorted `<ul>`.
TRY THIS: Render `<SortableList />` and click the sort button.
EXPECTED OUTPUT: List flips between ascending and descending alphabetical order.
CHECK: python3 check.py medium/p03
*/
// TODO: write your component from scratch
