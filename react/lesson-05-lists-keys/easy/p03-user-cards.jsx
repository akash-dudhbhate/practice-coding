/*
LESSON 05 — Lists & Keys
EASY P03 — User Cards from Array
============================================
CONCEPT: map() isn't just for `<li>` — each object can become a whole card of nested JSX, still keyed by its id.
PROBLEM: Build a `UserCards` component. Define a `users` array of `{ id, name, email }` and map each to a bordered `<div key={u.id}>` containing an `<h3>` name and `<p>` email.
TRY THIS: Render `<UserCards />`.
EXPECTED OUTPUT: Three stacked cards showing Alice, Bob, Charlie with their emails.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
