/*
LESSON 05 — Lists & Keys
EASY P01 — Fruit List
============================================
CONCEPT: .map() turns an array of data into an array of JSX. `key` on the outer element gives React a stable identity for each row.
PROBLEM: Build a `FruitList` component. Define a `fruits` array of `{ id, name }` objects and render a `<ul>` where each fruit maps to `<li key={f.id}>{f.name}</li>`.
TRY THIS: Render `<FruitList />`.
EXPECTED OUTPUT: A bullet list: Apple, Banana, Cherry.
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
