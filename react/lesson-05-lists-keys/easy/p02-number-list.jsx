/*
LESSON 05 — Lists & Keys
EASY P02 — Number List
============================================
CONCEPT: For a static list that never reorders, the array index is an acceptable key — pass it as map's second argument.
PROBLEM: Build a `NumberList` component. Create numbers 1-10 (e.g. `Array.from({ length: 10 }, (_, i) => i + 1)`) and render each as `<li key={i}>{n}</li>` inside a `<ul>`.
TRY THIS: Render `<NumberList />`.
EXPECTED OUTPUT: A numbered-looking bullet list of 1 through 10.
CHECK: python3 check.py easy/p02
*/
// TODO: write your component from scratch
