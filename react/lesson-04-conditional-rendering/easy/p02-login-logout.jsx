/*
LESSON 04 — Conditional Rendering
EASY P02 — Login/Logout Button
============================================
CONCEPT: The ternary `cond ? A : B` picks between two values inside JSX — perfect for either/or UI like Login vs Logout.
PROBLEM: Build a `LoginLogout` component with `loggedIn` boolean state. One button toggles it and shows `loggedIn ? "Logout" : "Login"` as its label.
TRY THIS: Render `<LoginLogout />` and click the button.
EXPECTED OUTPUT: Button label flips between "Login" and "Logout".
CHECK: python3 check.py easy/p02
*/
// TODO: write your component from scratch
