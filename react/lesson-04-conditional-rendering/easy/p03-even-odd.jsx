/*
LESSON 04 — Conditional Rendering
EASY P03 — Even/Odd Display
============================================
CONCEPT: Conditional text works too: `num % 2 === 0 ? "Even" : "Odd"` computes the label during render from state.
PROBLEM: Build an `EvenOdd` component with `num` state. Render a number input (`value`/`onChange` with `Number(e.target.value)`) and a `<p>` showing "Even" or "Odd" via the modulo ternary.
TRY THIS: Render `<EvenOdd />`, type 4, then 7.
EXPECTED OUTPUT: Paragraph reads "Even" for 4 and "Odd" for 7.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
