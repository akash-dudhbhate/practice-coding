/*
LESSON 04 — Conditional Rendering
EASY P01 — Show/Hide Toggle
============================================
CONCEPT: `{cond && <jsx>}` renders the JSX only when cond is truthy — the shortest way to show/hide a piece of UI.
PROBLEM: Build a `ShowHide` component with `show` boolean state. A button toggles it (label switches between "Show"/"Hide"); `{show && <p>Hello, I'm visible!</p>}` renders the message only when true.
TRY THIS: Render `<ShowHide />` and click the button twice.
EXPECTED OUTPUT: First click reveals "Hello, I'm visible!"; second hides it again.
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
