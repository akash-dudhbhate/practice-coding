/*
LESSON 04 — Conditional Rendering
MEDIUM P02 — Empty List Message
============================================
CONCEPT: `list.length === 0 ? <empty/> : <list/>` handles the empty state explicitly instead of silently rendering nothing.
PROBLEM: Build an `EmptyList` component with `todos` state starting empty. An "Add" button appends `Item N`; below, show "No todos yet!" when the array is empty, else a `<ul>` of items via `.map()`.
TRY THIS: Render `<EmptyList />`, note the message, click Add twice.
EXPECTED OUTPUT: "No todos yet!" is replaced by a two-item list.
CHECK: python3 check.py medium/p02
*/
// TODO: write your component from scratch
