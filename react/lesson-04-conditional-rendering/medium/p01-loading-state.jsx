/*
LESSON 04 — Conditional Rendering
MEDIUM P01 — Loading State
============================================
CONCEPT: An early `if (loading) return <spinner>` before the main return keeps the happy-path JSX clean — a very common fetch pattern.
PROBLEM: Build a `LoadingState` component with `loading` state starting true. If loading, return a "Loading..." paragraph plus a "Simulate Loaded" button that sets it false; otherwise return "Content loaded!".
TRY THIS: Render `<LoadingState />` and click "Simulate Loaded".
EXPECTED OUTPUT: "Loading..." is replaced by "Content loaded!".
CHECK: python3 check.py medium/p01
*/
// TODO: write your component from scratch
