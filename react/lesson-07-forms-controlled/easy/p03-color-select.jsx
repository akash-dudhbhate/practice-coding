/*
LESSON 07 — Forms & Controlled Inputs
EASY P03 — Select Dropdown
============================================
CONCEPT: In React, `<select>` is controlled by `value` on the select itself — not `selected` on the option.
PROBLEM: Build a `ColorSelect` with `color` state (start "blue"). Render `<select value={color} onChange={...}>` with red/green/blue `<option>`s and a `<p>` showing the pick.
TRY THIS: Render `<ColorSelect />` and choose Green.
EXPECTED OUTPUT: "Selected: green" appears below the dropdown.
CHECK: python3 check.py easy/p03
*/
// TODO: write your component from scratch
