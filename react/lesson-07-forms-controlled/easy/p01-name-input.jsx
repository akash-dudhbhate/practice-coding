/*
LESSON 07 — Forms & Controlled Inputs
EASY P01 — Name Input
============================================
CONCEPT: A controlled input wires `value={state}` + `onChange` — React owns the text, so the UI can react to every keystroke.
PROBLEM: Build a `NameInput` component with `name` state. Render a text input (`value`/`onChange` with `e.target.value`) and a `<p>` saying "Hello, {name}!".
TRY THIS: Render `<NameInput />` and type "Sam".
EXPECTED OUTPUT: The greeting updates to "Hello, Sam!" live.
CHECK: python3 check.py easy/p01
*/
// TODO: write your component from scratch
