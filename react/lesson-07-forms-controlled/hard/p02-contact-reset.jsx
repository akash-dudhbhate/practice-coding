/*
LESSON 07 — Forms & Controlled Inputs
HARD P02 — Controlled Form with Reset
============================================
CONCEPT: Because inputs are controlled, resetting = setting state back to the initial object — the inputs clear themselves.
PROBLEM: Build a `ContactForm` with an `initial` object, `form` and `success` state. On submit: preventDefault, set success true, `setForm(initial)`, and hide the success message after ~3s via setTimeout. Render name/email/message fields + a green success `<p>`.
TRY THIS: Render `<ContactForm />`, fill it, press Send.
EXPECTED OUTPUT: Fields clear instantly and "Message sent successfully!" shows for 3s.
CHECK: python3 check.py hard/p02
*/
// TODO: write your component from scratch
