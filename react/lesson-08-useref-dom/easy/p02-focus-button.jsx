/*
LESSON 08 — useRef & DOM Access
EASY P02 — Focus Input on Click
============================================
CONCEPT: Refs also work in event handlers — `inputRef.current.focus()` imperatively moves focus without any state.
PROBLEM: Build a `FocusOnButton` component. Create `inputRef`, attach it to a text input, and render a button whose `onClick` focuses the input via the ref.
TRY THIS: Render `<FocusOnButton />` and click the button.
EXPECTED OUTPUT: Focus jumps into the input on each click.
CHECK: python3 check.py easy/p02
*/
// TODO: write your component from scratch
