<!--
LESSON 01 — Quasar Basics
EASY P03 — Toggle Visibility
============================================
CONCEPT: a ref is a reactive value — when it changes, the template
re-renders. v-if adds/removes an element from the DOM, and @click
runs code when a button is clicked.

PROBLEM: Create a boolean ref (e.g. `visible`, starting false) and
a <q-btn> whose @click flips it (visible = !visible). Show a message
element with v-if="visible" that appears/disappears. Bonus: bind the
button label/color to the state (:label="visible ? 'Hide' : 'Show'").

TRY THIS: click the button — the message toggles. Change the initial
value of the ref to true so it starts visible.

EXPECTED OUTPUT: a button; clicking it shows and hides a message.

CHECK: python3 check.py easy/p03
-->
<template>
  <!-- TODO: write your template here -->
</template>

<script setup>
// TODO: write your script here
</script>
