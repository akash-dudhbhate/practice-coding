<!--
LESSON 04 — Vue Composition API
EASY P03 — Computed Full Name
============================================
CONCEPT: computed() creates a cached derived value — it only re-runs when a
dependency changes. Use it for anything you can derive from other state
instead of storing a copy.

PROBLEM: Two refs `firstName` and `lastName`, a computed `fullName` that
joins them, and two q-input fields to edit each name.

TRY THIS: const fullName = computed(() => `${firstName.value} ${lastName.value}`);
inputs with v-model="firstName" / v-model="lastName"

EXPECTED OUTPUT: The full name displayed at the top; typing in either input
updates it immediately.

CHECK: python3 check.py easy/p03
-->
<template>
  <!-- TODO: write your template here -->
</template>

<script setup>
// TODO: write your script here
</script>
