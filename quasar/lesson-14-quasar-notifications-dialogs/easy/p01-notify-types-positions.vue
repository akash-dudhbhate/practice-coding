<!--
LESSON 14 — Quasar Notifications & Dialogs
EASY P01 — Notify Types with Icons & Positions
============================================
CONCEPT: $q.notify options go beyond type — `icon` sets the glyph and
`position` picks where the toast lands (top-right, bottom, center...).
Four buttons, four combinations.

PROBLEM: 4 q-btns firing $q.notify — positive, negative, warning, info —
each with a custom message, its own icon, and a different position.

TRY THIS: $q.notify({ type: 'positive', message: 'Done!', icon: 'check_circle',
  position: 'top-right', timeout: 3000 })

EXPECTED OUTPUT: Each button pops a differently colored/iconed toast in a
different screen position.

CHECK: python3 check.py easy/p01
-->
<template>
  <!-- TODO: write your template here -->
</template>

<script setup>
// TODO: write your script here
</script>
