<!--
LESSON 08 — Quasar Plugins
EASY P03 — Platform Detection
============================================
CONCEPT: $q.platform.is tells you what you're running on: .mobile, .desktop,
.ios, .android, and browser flags like .chrome/.firefox/.safari/.edge. Use
computeds to display them.

PROBLEM: A platform info card listing three rows — device type
(Mobile/Desktop), OS (iOS/Android/Other), and browser name — all read from
$q.platform.is.

TRY THIS: const isMobile = computed(() => $q.platform.is.mobile);
browser via if (p.is.chrome) return 'Chrome' ... etc.

EXPECTED OUTPUT: A card with three badge rows identifying device, OS and
browser for the current visitor.

CHECK: python3 check.py easy/p03
-->
<template>
  <!-- TODO: write your template here -->
</template>

<script setup>
// TODO: write your script here
</script>
